# CityLBM v0.4.0-rc9 Release Notes

Status: accuracy diagnostic release candidate. This is not a formal v0.4.0 release.

## Newly Run Evidence

- Rebuilt CityLBM Release after adding the diagnostic z-origin switch.
- Added and ran `build_chain_audit.py`.
- Attempted Visual Studio Build Tools 2022 C++ installation with `winget`; the attempt failed and is recorded as a blocker.
- Regenerated the Case E validation report and release gate.

Evidence:

- `CityLBM/src/Components/Simulation/RunSimulationComponent.cs`
- `CityLBM/src/Core/FluidX3DInterface.cs`
- `CityLBM/citylbm.config`
- `docs/experiments/casee/tools/build_chain_audit.py`
- `docs/experiments/casee/results/build_chain_manifest.json`
- `docs/experiments/casee/results/citylbm_build_check.log`
- `docs/experiments/casee/results/casee_validation_report.md`
- `docs/experiments/casee/results/release_gate.json`

## Software Changes

- Added `Diagnostic Z Origin Offset` (`zOff`) to the Grasshopper `Run Simulation` component.
- Added `SimulationSettings.DiagnosticZOriginOffsetM`.
- Generated FluidX3D setup code now uses the effective z origin for inlet-profile height mapping.
- `citylbm_run_manifest.json` now records `diagnostic_z_origin_offset_m` and `effective_origin_z_m`.
- Case E probe-protocol risk metadata now uses the effective diagnostic z origin when reporting z=2 m lattice placement.

This switch defaults to 0 m. It is an experimental diagnostic control, not a default accuracy model.

## Build Chain Status

- .NET SDK: ready, `E:\citylbm_buildchain\dotnet\dotnet.exe`, SDK 8.0.423.
- FluidX3D binary: ready for existing-binary runs, `E:\citylbm_buildchain\FluidX3D\bin\FluidX3D.exe`.
- Visual Studio Build Tools 2022 C++: blocked.
- VS install attempt: `winget` exit code 1602.
- Recorded blockers: possible declined UAC prompt and insufficient C: free space for VS precheck.
- GPU runtime: `nvidia-smi` reports GPU lost; further long FluidX3D runs require GPU/driver recovery.

## Case E Metrics

No new CFD accuracy run was completed in rc9. The current latest official z=2 m metric remains the rc8 z-center diagnostic:

- n = 80, formal `raw_trilinear`.
- MAE = 21.111 pp.
- RMSE = 27.721 pp.
- Bias = -16.409 pp.
- R2 = -2.006330.
- Pearson = 0.115756.

## Release Gate

Formal `v0.4.0` remains blocked:

- official z=2 m R2 is still negative;
- MAE remains above the release threshold;
- Rhino/Grasshopper loading of the new GHA is not independently verified;
- VS Build Tools C++ and GPU runtime are not fully ready for further native validation.

This rc supports reproducibility, build-chain transparency, and diagnostic-method limitations. It does not support a claim that CityLBM has reached research-grade predictive accuracy for AIJ Case E.
