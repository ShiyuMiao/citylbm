# CityLBM v0.4.0-rc4 Release Notes

Status: accuracy diagnostic release candidate. This is not a formal v0.4.0 release.

## Newly Run Evidence

- AIJ Case A smoke regression passed on the current machine: dx = 3.5 m, 2000 FluidX3D steps, completed run log, and timestep-2000 VTK output present.
- Case A generated case, compile log, run log, artifact manifest, smoke-regression JSON, and external VTK manifest are stored under `docs/experiments/casea/`.
- Case E release gate now reads `docs/experiments/casea/results/casea_smoke_regression.json` and marks the Case A smoke-regression guard as passed.

Case E official z=2 m metrics remain unchanged:

- Best run: `dx2_gshift1_nu001`
- n = 80 official `ac+N` probes
- MAE = 23.972 percentage points
- RMSE = 29.095 percentage points
- Bias = -20.833 percentage points
- R2 = -2.311768
- Pearson = 0.071789

## Included Software and Audit Changes

- Added `docs/experiments/casea/tools/casea_smoke_audit.py` for generating, deploying, and auditing a minimal native AIJ Case A FluidX3D smoke case.
- Added lightweight Case A manifests so large VTK outputs remain outside Git while their paths, sizes, hashes, and timestamps are recorded.
- Extended the Case E release gate to include the Case A smoke-regression status.

## Default vs Experimental

Default-safe:

- Build-chain fallback and manifest/audit reporting.
- Fail-closed release gate.
- Solid-corner risk reporting.
- Case A smoke-regression guard.

Preset-only:

- AIJ Case E `ac+N` constants, AF profile scaling, official z=2 m probe target.

Experimental only:

- wind-direction audit (`+Y` vs `-Y`);
- no-SUBGRID diagnostic run;
- sample interval changes for CPU-side probe extraction;
- near-wall, rough-wall, effective-ground, `nu_lbm`, and alternative probe sampling modes.

## Manuscript Use

Paper-ready:

- Dataset provenance, hashes, and official 80-probe z=2 m protocol.
- Reproducible current-machine build/runtime evidence.
- Case A smoke regression as a workflow non-regression guard.
- Negative diagnostic finding: the current CityLBM/FluidX3D Case E pipeline does not yet meet official z=2 m accuracy requirements.
- Solid-corner diagnostic as a limitation/protocol-risk analysis.

Limitations only:

- Current Case E accuracy metrics.
- Directional improvement from effective-ground and lower `nu_lbm` diagnostics; it is not enough for a formal validation claim.
- Any claim of predictive accuracy, mesh independence, LES improvement, or validated v0.4.0 performance.

## Release Gate

Formal `v0.4.0` must not be tagged until the official z=2 m metric gate passes and Rhino/Grasshopper loads the new GHA. In this rc, Case A smoke regression passes, but official Case E accuracy and Rhino/GHA evidence still block the formal release.
