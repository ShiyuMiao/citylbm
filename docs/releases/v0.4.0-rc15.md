# CityLBM v0.4.0-rc15 Release Notes

Status: reproducibility-suite release candidate. This is not a formal v0.4.0 release.

## Newly Run Evidence

- Added and ran `reproducibility_suite.py`.
- Generated `casee_reproducibility_suite.json` and `casee_reproducibility_suite.md`.
- Rebuilt CityLBM in Release configuration and synchronized `CityLBM/bin/CityLBM.gha` from the Release output.
- Re-ran Case E audit, manuscript evidence summary, plugin identity gate, artifact index, paper evidence gate, and the expected-blocked formal release gate.

## Suite Verdict

The reproducibility suite passes because the current rc evidence chain is internally reproducible and claim-safe:

- CityLBM builds in Release configuration.
- The tracked `CityLBM.gha` matches the current Release build output; SHA256 = `13b3c76c2c9c7fd918dbf072227c65a2d8d856d6b3e9b3939494ed922db79b30`.
- Case E audit reproduces the current official z=2 m metrics.
- Paper evidence gate passes.
- Artifact index is present and covers required release/paper evidence files.
- Formal release gate remains blocked, as expected.

Artifact index summary:

- artifact count = 112;
- lightweight release assets = 80;
- optional log assets = 7;
- external-reference hash-only records = 2;
- formal accuracy claim supported = false.

## Case E Metrics

No new CFD accuracy improvement was produced in this rc. The formal official z=2 m metric remains:

- n = 80, formal `raw_trilinear`.
- MAE = 21.111 pp.
- RMSE = 27.721 pp.
- Bias = -16.409 pp.
- R2 = -2.006330.
- Pearson = 0.115756.

## Release Gate

Formal `v0.4.0` remains blocked:

- official z=2 m R2 is negative;
- MAE remains above the release threshold;
- Rhino/Grasshopper loading of the newly built GHA is not independently verified;
- VS Build Tools C++ and GPU runtime remain blockers for additional native validation.
