# CityLBM v0.4.0-rc14 Release Notes

Status: artifact-index and manuscript-traceability release candidate. This is not a formal v0.4.0 release.

## Newly Run Evidence

- Added and ran `artifact_index.py`.
- Generated `casee_artifact_index.csv`, `casee_artifact_index.json`, and `casee_artifact_index.md`.
- Re-ran the Case E audit, manuscript claim matrix, paper evidence gate, and plugin identity gate.

## Artifact Index

The artifact index records SHA256 hashes, byte sizes, git-tracking status, release-asset role, source evidence type, claim readiness, paper use, and limitations for Case E evidence files.

Current summary:

- artifact count = 109;
- lightweight release assets = 77;
- optional log assets = 7;
- external-reference hash-only records = 2;
- formal accuracy claim supported = false.

Large or source-data-heavy files are not promoted as ordinary Git evidence. The official `BD_caseE.stl` and `LF_caseE.xls` are recorded by hash as external/source references; long FluidX3D logs are optional log assets.

The synchronized tracked plugin asset is `CityLBM/bin/CityLBM.gha`, SHA256 = `fbce3e982643872d7261a7aa4d0dc10ad010e2525b756af2f6d838322be1b368`.

## Case E Metrics

No new CFD accuracy improvement was produced in this rc. The formal official z=2 m metric remains:

- n = 80, formal `raw_trilinear`.
- MAE = 21.111 pp.
- RMSE = 27.721 pp.
- Bias = -16.409 pp.
- R2 = -2.006330.
- Pearson = 0.115756.

## Paper Evidence Status

The paper evidence gate now requires:

- a negative official z=2 m validation status in `release_gate.json`;
- a manuscript claim matrix with blocked formal-release and negative-validation claims;
- no forbidden success claims in the Case E draft text;
- an artifact index containing key software, metric, report, gate, and release-note files.

The paper evidence gate passes under this bounded interpretation. This means the manuscript materials are traceable and claim-safe, not that CFD predictive accuracy has been validated.

## Release Gate

Formal `v0.4.0` remains blocked:

- official z=2 m R2 is negative;
- MAE remains above the release threshold;
- Rhino/Grasshopper loading of the newly built GHA is not independently verified;
- VS Build Tools C++ and GPU runtime remain blockers for additional native validation.
