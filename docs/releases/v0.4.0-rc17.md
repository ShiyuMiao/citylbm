# CityLBM v0.4.0-rc17 Release Notes

Status: blocker-remediation planning release candidate. This is not a formal v0.4.0 release.

## Newly Run Evidence

- Added `casee_blocker_remediation_plan.py`.
- Generated:
  - `docs/experiments/casee/results/casee_remaining_blockers.json`
  - `docs/experiments/casee/results/casee_remaining_blockers.csv`
  - `docs/experiments/casee/results/casee_remaining_blockers.md`
- Integrated the blocker remediation plan into `reproducibility_suite.py`.
- Extended `artifact_index.py` and `paper_evidence_gate.py` so the remaining-blocker plan and rc17 release note are tracked as required evidence.

## Case E Metrics

No new CFD accuracy improvement was produced in this rc. The formal official z=2 m metric remains:

- n = 80, formal `raw_trilinear`.
- MAE = 21.111 pp.
- RMSE = 27.721 pp.
- Bias = -16.409 pp.
- R2 = -2.006330.
- Pearson = 0.115756.

## Remaining Release Blockers

Formal `v0.4.0` remains blocked.

The current remediation plan identifies five concrete blockers:

- official z=2 m metric gate failure;
- unverified Rhino/Grasshopper loading of the new GHA;
- GPU runtime failure (`nvidia-smi` reports GPU lost);
- incomplete Visual Studio Build Tools 2022 C++ chain;
- missing dx=1 m high-resolution official run, currently only a feasibility item.

## Boundary

This rc improves the path from the current negative validation result toward a future accuracy run. It does not add a new solver result, does not improve R2, and does not allow a formal `v0.4.0` tag.
